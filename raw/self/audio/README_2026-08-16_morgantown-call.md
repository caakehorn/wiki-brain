# `2026-08-16_Morgantown_St_call-recording.m4a` — provenance

Filed 2026-08-20. **Read this before citing the file.**

Original filename `Morgantown St.m4a`, supplied by the operator with the
statement: *"this is the audio referenced in the logs. i recorded this when i
was being called from annie's phone saturday night."*

## Container metadata

| Field | Value |
|---|---|
| Duration | `mvhd` 927.19 s = **15 min 27 s** (`mdhd` 927.24 s) |
| Container date atom | `2026-08-17T03:54:49Z` — **UTC**; = **2026-08-16 23:54:49 America/New_York** |
| `mvhd` creation / modification | 2026-08-17 03:54:50Z / 03:54:55Z |
| Title (`©nam`) | `Morgantown St` |
| Encoder (`©too`) | `com.apple.VoiceMemos (Dan's MacBook Pro (null))` |
| `voice-memo-uuid` | `5B51561B-0110-4FDD-98B8-B41DB654CFDD` |
| Brands | `M4A / isom` |
| Size / md5 | 13,097,702 bytes / `96bd3df46d4b0f4c5278cc9d6978621d` |
| Supplied-file SHA-256 | `f656b6abd5c676a001eaf1f9a207cbc1fad262b3dbc3e9b145618d2bc307a4ec` |

The supplied audio's byte size and MD5 match the values already recorded in
this provenance file. SHA-256 is now recorded as the stronger integrity
identifier for the supplied copy.

Two things follow from the metadata alone, before anyone interprets the
conversation:

1. **It was recorded on a Mac, not a phone**, which is consistent with a call
   taken on the desktop.
2. **`Morgantown St` is Apple's location-derived default memo name** — it
   names where the *recording device* was. 463 Morgantown St is Dan's own
   residence (`wiki/legal/463-morgantown`); the other party's address of
   record is 106 Nassau St, Uniontown.

## Timing

The file's date atom is the **save** time. Aligning two spoken lines in the
previous secondary transcript against timestamped iMessages
(`raw/self/message-csv/imessage_export_2124702449_20260820.csv`) puts audio
t₀ at **23:36:30–23:38:07 EDT**, so the call ran roughly **23:37 → 23:53** and
was written to disk about two minutes later. Method and both alignments:
`wiki/timeline/events/august-2026-morgantown-call`.

## Direct transcription now filed

The operator has now supplied a timestamped speaker-labelled transcript
alongside the audio. It is filed verbatim at:

`raw/self/audio/2026-08-16_Morgantown_St_call-transcript.txt`

The supplied transcript contains 611 lines and runs from `00:00:00` through
`00:14:59`. The audio itself is 15:27.24, so the transcript does not purport
to cover the final ~28 seconds. The transcript is a **source-derived
transcription artifact**, not an independent acoustic authentication of every
word or speaker label.

The prior Hermes/PDF transcript remains useful as a secondary comparison
source, but it is no longer the only transcript representation in the corpus.

## Verification scope

This update establishes that concrete audio and concrete timestamped
transcript source material have been supplied and filed. It does **not** by
itself establish that the audio is an unedited original, nor does it
independently authenticate every transcript word or speaker attribution.
Those are separate forensic questions. Exact-wording claims should be checked
against the audio when required.

See `raw/self/audio/2026-08-20_Morgantown_St-source-verification.md` for the
full file-level checks and the distinction between provenance, transcription,
and independent authentication.
