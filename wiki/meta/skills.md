---
domain: meta
page_type: dataset
title: "The Skills Database"
status: active
knowledge: derived
date_created: 2026-08-30
date_modified: 2026-08-30
chart:
  kind: bar
  title: "Capabilities on the record, by kind"
  x: { label: "Kind", type: category }
  y: { label: "Live capabilities", type: number }
  series:
    - name: "Declared"
      points:
        "Skills": 17
        "MCP servers": 2
        "Tools": 2
        "Commands": 27
        "Subagents": 2
        "Harnesses": 1
sources: []
---

# The Skills Database

_Generated by `bin/wiki-skills page` from `skills/registry/events.jsonl`.
Do not hand-edit — rerun the tool. The append-only log is the record;
this page and `skills/registry/registry.json` are both derived from it._

[[wiki/meta/index|meta]] · [[wiki/meta/digest|Digest]] · [[wiki/meta/recent-activity|Recent]] · [[wiki/meta/open-questions|Open]]

Every other page in this wiki is about Dan Frank. This one is about the
machines that write them.

Several different models work on this repository — Claude Code in a
terminal, Claude in a browser, Codex, Cursor, whatever comes next — and
each arrives with a different set of skills, MCP servers, plugin tools and
subagents. None of them can see the others'. Without a shared record, every
session rediscovers the same surface from scratch, and a lesson one model
learned about a tool the next model also has dies in a transcript nobody
keeps. This is the shared record: a running database that any model can read
before it starts and write to when it finishes, so the capabilities
accumulate the way the rest of the corpus does.

It is public for the same reason the wiki is. A capability list nobody
outside can read is a capability list that cannot be checked.

## What it holds

| | |
|---|---|
| Capabilities on the record | 51 |
| Declared by more than one model | 0 |
| Models on the record | 1 |
| Retired | 0 |
| Events in the log | 52 |
| Last push | 2026-08-30 |

| Kind | Live |
|---|---|
| Commands | 27 |
| Skills | 17 |
| MCP servers | 2 |
| Subagents | 2 |
| Tools | 2 |
| Harnesses | 1 |

## The models on the record

| Model | Vendor | Surface | Declares | First seen | Last push |
|---|---|---|---|---|---|
| `claude-code` | Anthropic | cli · web · desktop · ide | 51 | 2026-08-30 | 2026-08-30 |

## Shared surface

Capabilities more than one model declares. These are the ones where a
lesson learned by one model transfers to another without translation —
and therefore the ones worth writing a skill about.

No capability is yet declared by more than one model. That is what the
second push will change.

## Skills

| Name | What it is | Declared by | Where |
|---|---|---|---|
| `artifact-design` | Design calibration for published Artifact pages — load before writing one, including a Markdown one. | `claude-code` | https://code.claude.com/docs/en/skills |
| `code-review` | Review a diff, PR, branch or path for correctness bugs and simplification at a chosen effort level; --comment posts findings inline on a PR, --fix applies them. | `claude-code` | https://code.claude.com/docs/en/slash-commands |
| `corpus-read` | Read a large two-sided message corpus by hand, in date order, in small windows, and derive a timeline of real events plus a working-notes ledger. Use when asked to read, work through, continue, or resume a message recor… | `claude-code` | `.claude/skills/corpus-read/SKILL.md` |
| `dataviz` | Chart and dashboard design method — form heuristics, an accessible colour formula with a runnable validator, mark specs and interaction rules. Read before writing any chart code in any medium. | `claude-code` | https://code.claude.com/docs/en/skills |
| `docx` | Read, create and edit Word documents and templates, including tracked changes and comments. | `claude-code` | https://code.claude.com/docs/en/skills |
| `pdf` | Extract text and tables from PDFs, merge, split, fill forms, and OCR scanned documents. | `claude-code` | https://code.claude.com/docs/en/skills |
| `pptx` | Create, read and edit PowerPoint presentations, layouts, templates and speaker notes. | `claude-code` | https://code.claude.com/docs/en/skills |
| `security-review` | Security review of the pending changes on the current branch. | `claude-code` | https://code.claude.com/docs/en/slash-commands |
| `session-start-hook` | Set up a repository for Claude Code on the web — a SessionStart hook that installs dependencies so tests and linters can run in a fresh remote container. | `claude-code` | https://code.claude.com/docs/en/claude-code-on-the-web |
| `skill-creator` | Create, edit and evaluate skills; measure trigger accuracy and benchmark a skill's description against variance analysis. | `claude-code` | https://code.claude.com/docs/en/skills |
| `skills/repo/change-safety` | Validate behavior, not merely successful commands — triggers: editing commands; editing workflows; editing build logic; repository-wide behavior changes | `claude-code` | `skills/repo/change-safety.md` |
| `skills/repo/session-loop` | Preserve the repository session loop — triggers: any multi-step code or repository task | `claude-code` | `skills/repo/session-loop.md` |
| `update-config` | Configure the harness through settings.json — permissions, environment variables, and hooks. Automated 'whenever X happens' behaviour must be a hook; the harness runs those, not the model. | `claude-code` | https://code.claude.com/docs/en/hooks |
| `wiki-housekeeping` | The periodic LINT sweep over this wiki — the judgment half of housekeeping, after `bin/wiki-check` has run the mechanical half. Triage the standing lint and connect warnings, work the stale-premise cascade, hunt contrad… | `claude-code` | `.claude/skills/wiki-housekeeping/SKILL.md` |
| `wiki-rewrite` | Full wipe-and-rewrite of one or more existing wiki/ pages in this repository — re-researching every claim from raw/ rather than editing the prose in place, then rebuilding the page's typed edges, index entries, downstre… | `claude-code` | `.claude/skills/wiki-rewrite/SKILL.md` |
| `xlsx` | Read, create and repair spreadsheets and delimited files — formulas, formatting, charts, and restructuring messy tabular data. | `claude-code` | https://code.claude.com/docs/en/skills |

## MCP servers

| Name | What it is | Declared by | Where |
|---|---|---|---|
| `claude-code-remote` | Session and schedule control for remote runs: attach repositories, spawn and message sibling sessions, list environments, and create Routines (cron triggers) that fire a prompt into this session or a fresh one. | `claude-code` | declared inline by `claude-code` |
| `github` | The only GitHub access a remote Claude Code session has — there is no gh CLI and no direct API. Pull requests, issues, reviews, checks, file contents, branches, commits, code search. Tools are prefixed mcp__github__. | `claude-code` | https://github.com/github/github-mcp-server |

## Tools

| Name | What it is | Declared by | Where |
|---|---|---|---|
| `Artifact` | Publish an HTML page to a private, shareable URL on claude.ai, with optional runtime capabilities. Useful for handing the operator a rendered view of corpus data without deploying to the portal. | `claude-code` | https://claude.ai/code/artifacts |
| `WebSearch` | Search the web from inside a session. The one route to facts that are newer than the model's cutoff. | `claude-code` | declared inline by `claude-code` |

## Commands

| Name | What it is | Declared by | Where |
|---|---|---|---|
| `bin/build-site` | build-site — render the wiki/ Markdown tree into a clean static site. | `claude-code` | `bin/build-site` |
| `bin/capture` | capture — drop a fact, story, or file into the wiki inbox. | `claude-code` | `bin/capture` |
| `bin/export-corpus` | export-corpus — bundle the wiki into a single file for LLM ingestion. | `claude-code` | `bin/export-corpus` |
| `bin/ingest-apply` | ingest-apply — apply an LLM's ingestion response to the wiki. | `claude-code` | `bin/ingest-apply` |
| `bin/ingest-pack` | ingest-pack — bundle one inbox item into a paste-anywhere LLM prompt. | `claude-code` | `bin/ingest-pack` |
| `bin/intake` | intake — the intake ledger: finite units in, discrete events out, honest arithmetic. | `claude-code` | `bin/intake` |
| `bin/llm-publish` | llm-publish — build the LLM-readable static export of the wiki (llm/). | `claude-code` | `bin/llm-publish` |
| `bin/mine-messages` | mine-messages — corpus mining over the full iMessage dump. | `claude-code` | `bin/mine-messages` |
| `bin/psychometrics` | psychometrics — test the instrument scores against the message corpus. | `claude-code` | `bin/psychometrics` |
| `bin/source-index` | source-index — coverage index for the message-source archive. | `claude-code` | `bin/source-index` |
| `bin/text-metrics` | text-metrics — turn-level style measurement over the message corpus. | `claude-code` | `bin/text-metrics` |
| `bin/verify-master` | verify-master — the promotion gate for a candidate master message corpus. | `claude-code` | `bin/verify-master` |
| `bin/wiki-check` | wiki-check — the mechanical half of a housekeeping pass, in one command. | `claude-code` | `bin/wiki-check` |
| `bin/wiki-climb` | wiki-climb — altitude engine for the wiki. | `claude-code` | `bin/wiki-climb` |
| `bin/wiki-connect` | wiki-connect — connective-tissue engine for the wiki graph. | `claude-code` | `bin/wiki-connect` |
| `bin/wiki-digest` | wiki-digest — generate human-facing reading aids from the wiki. | `claude-code` | `bin/wiki-digest` |
| `bin/wiki-freshness` | wiki-freshness — is the generated corpus in sync with wiki/? | `claude-code` | `bin/wiki-freshness` |
| `bin/wiki-gaps` | wiki-gaps — answer an open gap and park the answer for the next pass. | `claude-code` | `bin/wiki-gaps` |
| `bin/wiki-lint` | Mechanical lint for the personal wiki. Exits 1 if errors found. | `claude-code` | `bin/wiki-lint` |
| `bin/wiki-plain` | wiki-plain — the READER'S DIGEST layer: one plain-language twin per page. | `claude-code` | `bin/wiki-plain` |
| `bin/wiki-search` | wiki-search — repository command | `claude-code` | `bin/wiki-search` |
| `bin/wiki-skills` | wiki-skills — the cross-model skills database, and its public-facing page. | `claude-code` | `bin/wiki-skills` |
| `bin/wiki-status` | wiki-status — repository command | `claude-code` | `bin/wiki-status` |
| `bin/wiki-timeline` | wiki-timeline — build the master chronological index from wiki/ pages. | `claude-code` | `bin/wiki-timeline` |
| `bin/wiki-tui` | wiki-tui — a tiny full-screen capture terminal for the wiki-brain. | `claude-code` | `bin/wiki-tui` |
| `bin/wiki-work` | wiki-work — the one list of outstanding work, and it is mandatory. | `claude-code` | `bin/wiki-work` |

## Subagents

| Name | What it is | Declared by | Where |
|---|---|---|---|
| `Explore` | Read-only fan-out search agent for sweeping many files or naming conventions at once. Reads excerpts rather than whole files, so it locates code but does not audit it. | `claude-code` | https://code.claude.com/docs/en/sub-agents |
| `general-purpose` | Full-tool agent for multi-step research and execution where the parent only needs the conclusion. | `claude-code` | https://code.claude.com/docs/en/sub-agents |

## Harnesses

| Name | What it is | Declared by | Where |
|---|---|---|---|
| `claude-code` | Anthropic's coding agent: shell, file read/write, edit, glob, grep, task delegation, scheduled triggers and artifact publishing. Runs as a terminal CLI, a desktop app, an IDE extension and a remote web session against a… | `claude-code` | https://claude.com/claude-code |

## Recently revised

A capability changes when the model declaring it reports different
content — a rewritten instruction, a new trigger, a different tool
surface. The log keeps both digests, so a revision is visible as one.

Nothing has been revised yet — every capability is at its first digest.

## How a model pushes into this

When a model is told to update the skills in the wiki, this is the
operation. It runs in the `wiki-brain` repository:

```bash
bin/wiki-skills push --scan --agent <id> --vendor <vendor> --surface <surface>
bin/wiki-skills push -f manifest.json      # everything the scan cannot see
bin/wiki-skills page && bin/wiki-skills check
```

`--scan` reads what this repository itself supplies: the skills under
`.claude/skills/` and `skills/`, the commands in `bin/`, MCP servers
configured in the tree, hooks, and subagent definitions. The manifest
carries the other half — the skills, MCP servers, plugin tools and
harness a model has that nothing in the working tree records. The full
format is in `skills/registry/README.md`.

A push is idempotent: re-declaring a capability whose content has not
changed appends nothing to the log.

## What is deliberately not here

**No credential values, ever.** This repository is public and its history
cannot be un-published. An MCP server is recorded with its transport, its
command, the shape of its arguments and the *names* of the environment
variables it wants — never a value. `bin/wiki-skills` refuses a push
containing anything shaped like a key, a token or a URL with credentials
in it, and names the field rather than quietly stripping it. The gate
re-scans the committed database, so a hand-edit cannot get one past the
writer.

**Some rows are held back under the standing directive in `CLAUDE.md`.**
That directive stops new public writing about a living person; it does
not delete what already exists. So the reach here is as narrow as it can
be, and the hole is drawn at its true width rather than closed over:

- **2** capabilities are omitted from the tables entirely — the name itself cannot be printed
  here. They remain on the record in `skills/registry/` and are counted
  in every total above.

**No prompts, personalities or session state.** A capability is something a
model can invoke. Where the work stands is `LLM_HANDOFF.md`; what is
outstanding is `WORK.md`; what agents have *learned* about working here is
`skills/` itself, which this database indexes but does not replace.

