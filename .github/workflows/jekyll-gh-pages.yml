# Disabled: agent feed is deployed by deploy-agent-site.yml instead.
# Kept as an inactive override so the old Jekyll Pages sample does not fight
# for the same github-pages environment.
name: Deploy Jekyll with GitHub Pages dependencies preinstalled

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "This workflow is disabled; use Deploy agent site"
        required: false

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - name: Jekyll Pages retired
        run: |
          echo "Use workflow 'Deploy agent site' (deploy-agent-site.yml)."
          echo "This Jekyll workflow is intentionally inactive."
